---
# defaults file for graylog-collector
graylog_server: 172.18.11.45 

graylog_tags:
   - linux

graylog_package_url: https://github.com/Graylog2/collector-sidecar/releases/download/0.1.0-beta.2

graylog_package_name: collector-sidecar-0.1.0-1.x86_64.rpm

graylog_package_dest: /usr/local/src

graylog_package_local: "{{graylog_package_dest}}/{{graylog_package_name}}"

graylog_download_package: "no" 
